---
wspkg::purgelist:
  - discount		# conflict with `markdown`
  - blas		# conflict with `openblas`, `atlas-lapsack-base`	
  - dos2unix		# conflict with `tofrodos`
  - python2-pillow	# conflict with `python2-imaging`
  - mtr			# conflict with `mtr-gtk`
  - cdrkit		# conflict with `cdrtools`
  - vim			# conflict with `gvim`
  - gcc 		# conflict with `gcc-multilib`
  - gcc-libs 		# conflict with `gcc-libs-multilib`
  - unrar		# conflict with `rar`
  - ruby-minitest	# conflict with `ruby`
  - haskell-fail # no longer available in official repo
  - haskell-regex-pcre-builtin # no longer available in official repo
